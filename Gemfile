source 'https://rubygems.org'

# Remove or comment out the github-pages gem line
# gem 'github-pages', group: :jekyll_plugins

# Add these instead
gem 'jekyll', '~> 4.3'
gem 'csv'
gem 'base64'
gem 'bigdecimal'
gem 'webrick'

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
end
